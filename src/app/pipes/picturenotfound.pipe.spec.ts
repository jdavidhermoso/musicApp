import { PicturenotfoundPipe } from './picturenotfound.pipe';

describe('PicturenotfoundPipe', () => {
  it('create an instance', () => {
    const pipe = new PicturenotfoundPipe();
    expect(pipe).toBeTruthy();
  });
});
